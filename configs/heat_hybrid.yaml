experiment: heat

model:
  n: 4
  nx: 2

optimizer:
  max_iter: 1

solver:
  mode: hybrid

quantum:
  shots: 64
  backend_mode: ibm
  backend_name: null
  spectral_points: 4
  delta: 0.001

scaling:
  sizes: [2, 4, 8]
  iterations: 17

plots:
  show_solution: true
  show_scaling: true

output:
  save_dir: plots2