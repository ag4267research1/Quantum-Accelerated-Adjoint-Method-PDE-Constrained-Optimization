experiment: heat

model:
  n: 100
  nx: 5

optimizer:
  max_iter: 1

solver:
  mode: hybrid

quantum:
  shots: 1024
  spectral_points: 16
  delta: 0.001

scaling:
  sizes: [16, 32, 64, 128]
  iterations: 5

plots:
  show_solution: true
  show_scaling: true